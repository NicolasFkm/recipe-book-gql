type Recipes { 
  id: ID!
  name: String!
  publishers: [Publisher]
  
  # Mandatory
  createdAt: Date!
  updatedAt: Date!
}

# Inputs
input RecipesCreateInput {
  name: String!
}

input RecipesUpdateInput {
  name: String
}