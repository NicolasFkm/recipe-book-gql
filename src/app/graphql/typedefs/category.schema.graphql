schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Category {
  _id: ID!
  name: String!
  description: String
}

type Mutation {
  createCategory(name: String): Category
}

type Subscription {
  categoryCreated: Category!
}

type Query {
  categories: [Category]!
}
